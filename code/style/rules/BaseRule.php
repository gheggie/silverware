<?php

/**
 * An extension of the style rule class for a base rule.
 */
class BaseRule extends StyleRule
{
    private static $singular_name = "Base Rule";
    private static $plural_name   = "Base Rules";
}
